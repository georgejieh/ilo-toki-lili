# Infra

Infrastructure helpers live here: cloud training setup, startup scripts, and artifact sync.

The intended training target is a modest spot GPU machine with checkpoints and shards stored in object storage. Machines should be reproducible from scripts rather than hand-configured state.
