# World

The world engine generates small visual scenes and episodes for grounded learning.

Planned modules:

- `sampler.py`: scene graph sampling.
- `render.py`: deterministic 96x96 rendering.
- `physics.py`: event simulation and interventions.
- `sprites/`: parametric category renderers.
- `audit_diversity.py`: checks that visual categories are varied enough.
