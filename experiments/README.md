# Experiments

Use this directory for public, reproducible experiment definitions.

Initial experiment tracks:

1. `text_transformer`: text-only Toki Pona baseline.
2. `grounded_transformer`: standard transformer trained with multimodal grounding data.
3. `grounded_new_architecture`: alternate architecture trained on the same grounded data.

Private scratch plans and project memory should stay outside tracked files.
